# ============================================================
# NexTox — AI Chemical Toxicity Prediction Platform
# Python Requirements (pip install -r requirements.txt)
# Tested on Python 3.9 / 3.10 / 3.11
# ============================================================

# ── Core ML & Data Science ──────────────────────────────────
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1.post1
lightgbm==4.3.0
joblib==1.3.2

# ── Cheminformatics ──────────────────────────────────────────
# Option A (pip): rdkit-pypi
# Option B (conda): conda install -c conda-forge rdkit
rdkit==2023.9.5

# ── Generative AI / NLP ─────────────────────────────────────
transformers==4.39.3
torch==2.2.2
tokenizers>=0.15.0
sentencepiece>=0.1.99
accelerate>=0.27.0

# ── Streamlit Frontend ───────────────────────────────────────
streamlit==1.32.2

# ── Django REST Backend ──────────────────────────────────────
django==4.2.11
djangorestframework==3.15.1
django-cors-headers==4.3.1

# ── Image Processing ─────────────────────────────────────────
Pillow==10.2.0

# ── Utilities ────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.66.0

# ============================================================
# INSTALLATION NOTES
# ============================================================
# 1. Create a virtual environment first (recommended):
#    python -m venv venv
#    venv\Scripts\activate     (Windows)
#    source venv/bin/activate  (macOS/Linux)
#
# 2. Install all packages:
#    pip install -r requirements.txt
#
# 3. If RDKit fails via pip, install via conda instead:
#    conda install -c conda-forge rdkit
#
# 4. PyTorch: If you have an NVIDIA GPU and want CUDA support,
#    visit https://pytorch.org/get-started/locally/ and install
#    the correct torch version for your CUDA version.
#    The version listed above (2.2.2) is CPU-only compatible.
#
# 5. The FLAN-T5 model (~1GB) is pre-cached locally at:
#    MODELS/flatonmodel/flan_model/
#    No internet download required after cloning this repo.
# ============================================================
